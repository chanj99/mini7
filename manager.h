#include "product.h"

void listProduct( Product *p, int count);//목록 전체 읽기
void search(Product *p, int count); //검색 하기 기능
void saveData(Product *p, int count); //파일 저장 기능
int loadData(Product *p); //파일 불러오기 기능

